{
  "name": "kinweave",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Kinweave — persona-to-persona negotiation protocol: runnable spec + simulation",
  "scripts": {
    "sim": "tsx src/sim/run.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
